- model_class = League
div class="page-header"
  h1
    | #{@league.name}
    br
    small Owner: #{@league.owner.name}
    br
    small Level: #{@league.level}

ul.nav.nav-tabs
  li.active
    a data-toggle="tab" href="#leagues-tab" Leagues
  li
    a data-toggle="tab" href="#matches-tab" Matches

.tab-content
  #leagues-tab.tab-pane.fade.in.active
    - if @league.teams.any?
      == render 'leagueTable'
    - else
      h1 No teams available!

  #matches-tab.tab-pane.fade
    - if @matches.any?
      == render 'matchTables'
    - else
      h1 No matches available!
